<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="reviewMapper">

   <resultMap id="reviewResultSet"
      type="review">
      <result property="reviewNo" column="REVIEW_NO_PK"/>
      <result property="userNo" column="USER_NO_FK"/>
      <result property="bottleNo" column="BOTTLE_NO_FK" />
      <result property="grade" column="GRADE" />
      <result property="reviewTitle" column="REVIEW_TITLE" />
      <result property="reviewContent" column="REVIEW_CONTENT" />
      <result property="createDate" column="CREATE_DATE" />
      <result property="imgUrl" column="IMG_URL" />
      <result property="imgCusUrl" column="IMG_CUS_URL" />
      <result property="status" column="STATUS" />
   </resultMap>

   <select id="selectList" parameterType="Integer" resultMap="reviewResultSet">
       <![CDATA[   
         SELECT *
         FROM review_tb
         WHERE BOTTLE_NO_FK=#{bottleNo}
      ]]>
   </select>


	<select id="selectOne" parameterType="Integer" resultMap="reviewResultSet">
       <![CDATA[   
         SELECT *
         FROM review_tb
         WHERE REVIEW_NO_PK=#{reviewNo}
      ]]>
   </select>
	
	<update id="insertOne" parameterType="review">
       <![CDATA[   
         INSERT INTO review_tb(USER_NO_FK, BOTTLE_NO_FK, GRADE, REVIEW_TITLE, REVIEW_CONTENT, IMG_URL)
         VALUE(#{userNo}, #{bottleNo}, #{grade}, #{reviewTitle}, #{reviewContent}, #{imgUrl})
      ]]>
	</update>
	
	<update id="updateOne" parameterType="review">
       <![CDATA[
         UPDATE review_tb
         SET GRADE=#{grade}, REVIEW_TITLE=#{reviewTitle}, REVIEW_CONTENT=#{reviewContent}, IMG_URL=#{imgUrl}
         WHERE REVIEW_NO_PK=#{reviewNo}
      ]]>
	</update>
	
	<update id="deleteOne" parameterType="review">
       <![CDATA[
         UPDATE review_tb
         SET STATUS='N'
         WHERE REVIEW_NO_PK=#{reviewNo}
      ]]>
	</update>

</mapper>